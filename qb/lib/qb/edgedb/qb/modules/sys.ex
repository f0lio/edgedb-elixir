defmodule(QB.EdgeDB.QB.Modules.Sys) do
  [
    [
      nil,
      (
        execute_fn_name = :execute_get_version

        defp(unquote(execute_fn_name)(args)) do
        end
      )
    ],
    [
      nil,
      (
        execute_fn_name = :execute_get_version_as_str

        defp(unquote(execute_fn_name)(args)) do
        end
      )
    ],
    [
      nil,
      (
        execute_fn_name = :execute_get_current_database

        defp(unquote(execute_fn_name)(args)) do
        end
      )
    ],
    [
      nil,
      (
        execute_fn_name = :execute_get_transaction_isolation

        defp(unquote(execute_fn_name)(args)) do
        end
      )
    ]
  ]
end